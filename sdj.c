#include<stdio.h>

int main(){
    int TotalInput, UserInput, countNum, i=1;i<=TotalInput;i++,x<TotalInput;x++;
    char ch;
    do{
     printf("\nMENU\N");
     printf("[I]input\n");
     printf("[E]exit\n");
     printf("Choice:\n");
     scanf("%c",&ch);
     switch(ch){
            case 'I':
     printf("How many?");
     scanf("%d",&TotalInput);
     for(i=1;1<=TotalInput;i++){
     printf("Enter Number:",i);
     scanf("%d",&UserInput);
     break;
     case'D':
     for(x=0; x<TotalInput;x++){
     }printf("%d",UserInput[x]);

     }
}}while(ch!='E'&&ch!='e');
return 0;
}
